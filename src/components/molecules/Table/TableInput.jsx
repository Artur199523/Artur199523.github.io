import React from "react";

const TableInput = () =>{
    return(
        <input
        onChange={onChange}
        value={value}
        max={max}
        min={max}
        view={view}
        />
    )
}

export default TableInput