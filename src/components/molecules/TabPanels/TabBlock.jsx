import React from "react"
import "./style.scss"

const TabBlock = ({children}) =>{

    return(
        <div className="tab-block">
            {children}
        </div>
    )
}

export default TabBlock